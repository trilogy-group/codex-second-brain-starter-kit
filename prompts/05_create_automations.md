# Prompt 05: Create The Automation Pack

Use `product-intelligence-factory` and `product-engineering-ops`.

Create the standard automation pack for this product manifest:
`/absolute/path/to/product.yaml`

I want the pack to include:
- source-truth sync
- PR merge sync
- repo mirror sync
- engineering-readiness audit

Keep the automations generic. Product-specific differences should live in the manifest and prompt parameters, not in custom logic.
